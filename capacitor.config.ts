import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.swisbidbuilder',
  appName: 'SWIS Bid Builder',
  webDir: 'dist'
};

export default config;
